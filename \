/*
 * Matthew Todd Geiger <mgeiger@newtonlabs.com>
 * 
 * Newton Laboratories, libnewton
 *
 * \\\\\\\\\\\\\\\\\\\
 *  \--\ galil.hpp \--\
 *   \\\\\\\\\\\\\\\\\\\
 */

#pragma once

#include <string>

#include <gclib.h>
#include <gclibo.h>

#include "macro.hpp"

namespace galil {
	class GalilController {
		public:
			GalilController(std::string address);
			~GalilController();

			void Move(uint32_t position);
			void Stop();
		private:
			void _Init();

			GCon _con = nullptr;
			bool _failure = false;
	};

} // namespace galil
